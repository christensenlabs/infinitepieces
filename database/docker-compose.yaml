services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: infinitepieces
      POSTGRES_USER: infinitepieces
      POSTGRES_PASSWORD: infinitepieces
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
